============
Installation
============

At the command line::

    $ easy_install oscar_payonline

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv oscar_payonline
    $ pip install oscar_payonline
